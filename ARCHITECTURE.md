# Polymarket 短线市场实时监控 — 架构思路

## 一、业务目标

在 Polymarket 上实时监控加密货币短线 Up/Down 市场，当某个 token 的买一价达到阈值时，自动以限价单买入，赌该方向在剩余时间内获胜。

**核心场景**：短线高频（5分钟～4小时），要求从行情变化到下单的延迟在秒级以内。

---

## 二、监控对象

### 市场 = 币种 × 周期

| 维度 | 内容 |
|------|------|
| 币种 | BTC, ETH, SOL, XRP, DOGE, BNB, HYPE 等 |
| 周期 | 5m / 15m / 1h / 4h |
| 方向 | Up（看涨）、Down（看跌） |

每个市场有 2 个 token（Up + Down），例如 `btc/5m` 市场包含 BTC-5min-Up 和 BTC-5min-Down 两个 token。

### 市场轮次

每个市场按周期划分为连续的"轮次"，每轮有固定起止时间：

```
[08:20]              [08:25]              [08:30]
   |——— 当前轮 ———————|——— 下一轮 ———————|
```

- 5m 市场：每小时 12 轮
- 15m 市场：每小时 4 轮
- 1h 市场：每天 24 轮
- 4h 市场：每天 6 轮

轮次到期后自动切换到下一轮，重新获取新 token 并开始监控。

---

## 三、数据流

```
  ┌──────────────────────────────────────────────┐
  │            Polymarket 实时行情数据              │
  │   (每个市场独立订阅，故障隔离)                    │
  └────────────────────┬─────────────────────────┘
                       │
          ┌────────────┼────────────┐
          │            │            │
          ▼            ▼            ▼
     btc/5m       eth/15m      sol/4h
     (2 token)    (2 token)    (2 token)
          │            │            │
          ▼            ▼            ▼
    ┌──────────────────────────────────────┐
    │  best_bid >= 0.99 ?                  │
    │  ├─ Yes → 挂 BUY 限价单               │
    │  └─ No  → 忽略                        │
    └──────────────────────────────────────┘
```

---

## 四、市场 Slug 规律

Polymarket 短线市场有两种 slug 命名格式，取决于周期类型：

### 格式 A：时间戳型（5m / 15m / 4h）

```
{缩写}-updown-{周期}-{unix秒}

示例：
  btc-updown-5m-1781426100
  sol-updown-15m-1781425200
  eth-updown-4h-1781409600
```

- 时间戳 = `(UTC秒 // 周期间隔) × 周期间隔`
- 轮次连续，无间断

### 格式 B：日期型（1h）

```
{全称}-up-or-down-{月份}-{日期}-{年份}-{小时}{ampm}-et

示例：
  bitcoin-up-or-down-august-8-2026-4am-et
  ethereum-up-or-down-august-8-2026-8pm-et
```

- 时区：美东时间（EDT, UTC-4）
- 月份：英文全小写（august, september...）
- 小时：12 小时制
- 日期：无前导零

### 币种缩写 ↔ 全称映射

| 缩写 | 全称 |
|------|------|
| btc | bitcoin |
| eth | ethereum |
| sol | solana |
| xrp | xrp |
| doge | dogecoin |
| bnb | bnb |
| hype | hype |

---

## 五、实时数据

每个市场（币种+周期）需要独立获取实时行情数据，核心需求：

1. **故障隔离**：一个市场的行情源出问题不影响其他市场
2. **订阅精简**：每个市场只需订阅 2 个 token，数据量小
3. **独立恢复**：各市场按自己的节奏从异常中恢复，不互相拖累

需要关注的数据字段：

| 字段 | 说明 |
|------|------|
| `asset_id` | token ID，用于匹配 Up/Down 方向 |
| `best_bid` | 买一价，触发判断的核心指标 |
| `bids` / `asks` | 完整订单簿，首次加载时获取基准价格 |

### 超时检测

不同周期市场的数据更新频率差异很大，需要差异化设置无数据超时：

| 市场类型 | 无数据超时 | 说明 |
|----------|-----------|------|
| 5m / 15m | 5 秒 | 高频市场，消息密集 |
| 1h / 4h | 300 秒 | 低频市场，可能长时间无成交 |

---

## 六、下单策略

### 触发条件（AND 关系）

1. `best_bid >= MIN_BID`（默认 0.99）
2. 该轮次内该 token 尚未下过单（去重）
3. 已过冷静期（见下）
4. 价格自动 clamp 到 0.99（CLOB 限价单上限）

### 冷静期（Cooldown）

防止在轮次初期价格尚未稳定时过早入场：

| 周期 | 冷静期 | 逻辑 |
|------|--------|------|
| 5m | 210s（3.5 分钟） | 轮次前 70% 时间不交易 |
| 15m | 660s（11 分钟） | 轮次前 73% 时间不交易 |
| 1h | 2700s（45 分钟） | 轮次前 75% 时间不交易 |
| 4h | 12600s（3.5 小时） | 轮次前 87.5% 时间不交易 |

设计意图：短线市场在临近结算时价格信号最有价值（Up/Down 趋近 0 或 1），过早入场容易被噪音误导。

### 下单参数

- **方向**：固定 BUY（买入该 token 即赌该方向赢）
- **类型**：GTC 限价单（Good Till Cancelled）
- **价格**：触发时的 best_bid，上限 0.99
- **份数**：按周期可配置不同金额

### 去重机制

- 每轮每个 token 最多下单一次
- 用 `ordered` 集合记录已下单的 token_id
- 下单失败时移除去重标记，允许重试
- 轮次切换时自动清空

### 并发控制

- 多个市场可能同时触发 → `asyncio.Lock` 确保下单请求串行化
- 避免并发请求撞 CLOB 限流

---

## 七、轮次生命周期

```
初始化 → 获取当前轮 token → 订阅实时数据 → 监控价格
                                          │
                    ┌─────────────────────┤
                    │                     │
              价格更新                  时间到期
                    │                     │
                    ▼                     ▼
              解析 best_bid          切换到下一轮
                    │                     │
              ┌────┴────┐          获取新 token
              │         │               │
         触发阈值   未触发         订阅新 token
              │         │               │
              ▼         ▼               ▼
          挂单 + 去重   忽略        继续监控...
```

---

## 八、容错设计

| 故障场景 | 应对策略 |
|----------|----------|
| 行情数据断流 | 超时检测 → 自动重连，指数退避（1s → 2s → ... → 30s 上限） |
| Gamma API 失败 | 重试 3 次（2s/4s 间隔） |
| 下单失败 | 打印错误 + 移除去重标记（允许重试） |
| 轮次获取失败 | 跳过该轮，等下一轮切换时再试 |
| 单市场故障 | 隔离：不影响其他市场 |

---

## 九、日志与可观测性

- **心跳日志**：每 60 秒打印一次（收消息数 + 已下单数）
- **触发日志**：每次触发下单写入 `triggers_YYYYMMDD.log`
- **调试模式**：可开关，打印每条原始行情数据
- **静默模式**：生产运行时仅输出触发信号，减少噪音

---

## 十、关键设计决策总结

1. **一市场一订阅**：每个市场独立获取行情数据，隔离故障、精简数据量
2. **轮次驱动**：以市场轮次为生命周期，到期自动切换，不需人工干预
3. **冷静期**：只在轮次后期交易，用时间换确定性
4. **去重优先**：每轮每 token 只下一次，避免重复下单
5. **串行下单**：全局 Lock 防止并发撞限流
6. **差异化超时**：适应不同周期市场的数据频率差异（高频 5s / 低频 300s）
7. **配置集中**：所有参数可集中调整（阈值、份数、币种列表）

---

## 十一、重构可考虑的方向

1. **新 SDK 集成**：新版 Polymarket SDK 可能内置了实时数据订阅和连接管理，可大幅简化代码
2. **MCP 服务**：如果有 MCP 服务可用，市场数据获取和下单可通过统一接口调用
3. **slug 构建**：如果 Gamma API 提供了更直接的轮次查询接口，可简化 slug 构建逻辑
4. **配置外部化**：考虑 YAML/TOML 配置文件替代代码内常量
5. **监控指标**：增加 Prometheus metrics 或结构化日志
6. **优雅关闭**：增加 signal handler，确保退出时清理所有资源
